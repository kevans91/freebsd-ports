--- channels/urbdrc/client/CMakeLists.txt.orig	2016-09-22 04:15:46 UTC
+++ channels/urbdrc/client/CMakeLists.txt
@@ -42,7 +42,7 @@ if (UDEV_FOUND AND UDEV_LIBRARIES)
 	set(${MODULE_PREFIX}_LIBS ${${MODULE_PREFIX}_LIBS} ${UDEV_LIBRARIES})
 endif()
 
-set(${MODULE_PREFIX}_LIBS ${${MODULE_PREFIX}_LIBS} winpr freerdp)
+set(${MODULE_PREFIX}_LIBS ${${MODULE_PREFIX}_LIBS} winpr2 freerdp2)
 
 target_link_libraries(${MODULE_NAME} ${PRIVATE_KEYWOARD} ${${MODULE_PREFIX}_LIBS})
 
