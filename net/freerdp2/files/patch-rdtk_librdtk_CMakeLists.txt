--- rdtk/librdtk/CMakeLists.txt.orig	2016-09-22 04:15:46 UTC
+++ rdtk/librdtk/CMakeLists.txt
@@ -38,8 +38,8 @@ set(${MODULE_PREFIX}_SRCS
 
 add_library(${MODULE_NAME} STATIC ${${MODULE_PREFIX}_SRCS})
 
-list(APPEND ${MODULE_PREFIX}_LIBS winpr)
-list(APPEND ${MODULE_PREFIX}_LIBS freerdp)
+list(APPEND ${MODULE_PREFIX}_LIBS winpr2)
+list(APPEND ${MODULE_PREFIX}_LIBS freerdp2)
 
 target_link_libraries(${MODULE_NAME} ${PRIVATE_KEYWORD} ${${MODULE_PREFIX}_LIBS})
 
