--- libfreerdp/core/test/CMakeLists.txt.orig	2016-09-22 04:15:46 UTC
+++ libfreerdp/core/test/CMakeLists.txt
@@ -25,7 +25,7 @@ add_executable(${MODULE_NAME} ${${MODULE
 add_definitions(-DTESTING_OUTPUT_DIRECTORY="${CMAKE_BINARY_DIR}")
 add_definitions(-DTESTING_SRC_DIRECTORY="${CMAKE_SOURCE_DIR}")
 
-target_link_libraries(${MODULE_NAME} freerdp winpr freerdp-client)
+target_link_libraries(${MODULE_NAME} freerdp2 winpr2 freerdp-client2)
 
 set_target_properties(${MODULE_NAME} PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${TESTING_OUTPUT_DIRECTORY}")
 
