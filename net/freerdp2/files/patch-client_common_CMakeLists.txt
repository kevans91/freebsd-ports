--- client/common/CMakeLists.txt.orig	2016-09-22 04:15:46 UTC
+++ client/common/CMakeLists.txt
@@ -15,7 +15,7 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
-set(MODULE_NAME "freerdp-client")
+set(MODULE_NAME "freerdp-client2")
 set(MODULE_PREFIX "FREERDP_CLIENT")
 
 #  Policy CMP0022: INTERFACE_LINK_LIBRARIES defines the link
@@ -62,7 +62,7 @@ if (WITH_LIBRARY_VERSIONING)
   set_target_properties(${MODULE_NAME} PROPERTIES VERSION ${FREERDP_VERSION} SOVERSION ${FREERDP_API_VERSION})
 endif()
 
-set(${MODULE_PREFIX}_LIBS ${${MODULE_PREFIX}_LIBS} winpr)
+set(${MODULE_PREFIX}_LIBS ${${MODULE_PREFIX}_LIBS} winpr2)
 
 target_link_libraries(${MODULE_NAME} ${PRIVATE_KEYWORD} ${FREERDP_CHANNELS_CLIENT_LIBS})
 if(OPENBSD)
