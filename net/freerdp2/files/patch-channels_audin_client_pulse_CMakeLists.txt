--- channels/audin/client/pulse/CMakeLists.txt.orig	2016-09-22 04:15:46 UTC
+++ channels/audin/client/pulse/CMakeLists.txt
@@ -26,6 +26,6 @@ include_directories(${PULSE_INCLUDE_DIR}
 add_channel_client_subsystem_library(${MODULE_PREFIX} ${MODULE_NAME} ${CHANNEL_NAME} "" TRUE "")
 
 
-set(${MODULE_PREFIX}_LIBS freerdp ${PULSE_LIBRARY} winpr)
+set(${MODULE_PREFIX}_LIBS freerdp2 ${PULSE_LIBRARY} winpr2)
 
 target_link_libraries(${MODULE_NAME} ${${MODULE_PREFIX}_LIBS})
