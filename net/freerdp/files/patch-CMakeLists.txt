--- CMakeLists.txt.orig	2016-08-04 03:44:26 UTC
+++ CMakeLists.txt
@@ -151,6 +151,11 @@ if(${CMAKE_SYSTEM_NAME} MATCHES "BSD")
 	endif()
 endif()
 
+if(${CMAKE_SYSTEM_NAME} MATCHES "DragonFly")
+       set(BSD TRUE)
+       set(FREEBSD TRUE)
+endif()
+
 # Configure MSVC Runtime
 if(MSVC)
 	include(MSVCRuntime)
